
TO install mysql
npm install --save mysql2

npm install --save sequelize

#MongoDb
add mongodb driver 
npm install --save mongodb

I hosted mongodb locally

start using mongoose
npm install --save mongoose
I also truncated the db

installing session package 
npm install --save express-session

adding a package to save session in mongodb
npm install --save connect-mongodb-session

installing bcryptjs package to hash our passwords
npm install --save bcryptjs

protecting our site from csrf attacks
npm install --save csurf

adding a package to handel messages to users
npm install --save connect-flash

adding package to send email
npm install --save nodemailer nodemailer-sendgrid-transport

adding validation package to project
npm install --save express validator

adding ability to add file to form
npm install --save multer

using pdfkit
npm install --save pdfkit